# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
.pytest_cache/
.ruff_cache/
.mypy_cache/
dist/
build/

# Environment
.env
.env.local

# Models — never commit weights
models/
*.gguf
*.safetensors
*.bin
*.pt

# Data — raw corpora and generated artifacts stay local
data/raw/*
!data/raw/.gitkeep
data/generated/*
!data/generated/.gitkeep
*.duckdb
*.sqlite
*.sqlite3

# Outputs
runs/
outputs/
*.log

# Node
node_modules/
.vite/
frontend/dist/

# OS / editor
.DS_Store
Thumbs.db
.idea/
.vscode/

# Icecat daily index: measured at 0/999 coverage on this catalog (see DIARY entry 4).
# Kept locally as evidence, not worth 1.3 MB in history.
daily.index.xml.gz

# Working notes kept on disk but out of the repo. The judge-facing documents are
# README.md, SUBMISSION.md and docs/RESULTS.md; these are the workings behind them.
DEMO-SCRIPT.md
CONTEXT.md
DEPLOY.md
DIARY.md
START-HERE.md
WINNING-PLAN.md
guide.md
plan.md
docs/HANDOFF.md

# The organisers' template, kept locally as the source for the built deck.
\[EXT\] UniHack-Protoype Template*.pptx

# Office lock files. Created while a document is open; never belong in the repo.
~$*
